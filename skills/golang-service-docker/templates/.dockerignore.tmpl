# Generated by golang-service-docker.

# Repository metadata, local secrets, editor state, and scratch output.
**/.git/
**/.env
**/.idea/
**/.vscode/
**/.worktrees/
**/*.tmp
**/*.log
**/coverage.out
**/coverage.html

# --- IF source ---
# Source mode compiles inside Docker and never needs host build output.
{{SOURCE_PREFIX}}bin/
# --- END source ---

# --- IF prebuilt ---
# Keep exactly the selected prebuilt binary in the build context. Do not use
# `bin/` here: excluding the parent directory would make the exception inert.
{{SOURCE_PREFIX}}bin/*
!{{SOURCE_PREFIX}}bin/{{BINARY_NAME}}
# --- END prebuilt ---
