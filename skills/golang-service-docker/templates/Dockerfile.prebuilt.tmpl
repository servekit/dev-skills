# Generated by service-docker-scaffold. Prebuilt mode intentionally packages
# only a caller-supplied, statically linked Linux ELF binary. The generated
# Makefile rejects host-native and dynamically linked inputs before building.
#
# Substitutions:
#   {{SOURCE_PREFIX}}      service path prefix relative to build context, or empty
#   {{HEALTH_PROBE_IMAGE}} image supplying the grpc_health_probe binary
#   {{RUNTIME_IMAGE}}      final runtime base image
#   {{BINARY_NAME}}        host bin filename and container executable name
#   {{EXPOSE_PORT}}        grpcx gRPC listen port
#   {{HTTP_PORT}}          optional grpc-gateway listen port
#   {{CONFIG_SOURCE}}      non-secret config template copied as /app/config.yaml

FROM {{HEALTH_PROBE_IMAGE}} AS grpc-health-probe

FROM {{RUNTIME_IMAGE}}
RUN apk add --no-cache ca-certificates tzdata \
    && adduser -D -u 10001 appuser

WORKDIR /app
COPY {{SOURCE_PREFIX}}bin/{{BINARY_NAME}} /app/{{BINARY_NAME}}
COPY --from=grpc-health-probe /ko-app/grpc-health-probe /usr/local/bin/grpc_health_probe
# --- IF config_copy ---
COPY {{SOURCE_PREFIX}}{{CONFIG_SOURCE}} /app/config.yaml
# --- END config_copy ---
RUN chmod 0755 /app/{{BINARY_NAME}}

USER appuser
EXPOSE {{EXPOSE_PORT}}
# --- IF gateway ---
EXPOSE {{HTTP_PORT}}
# --- END gateway ---
ENTRYPOINT ["/app/{{BINARY_NAME}}"]
