# Generated by `go build .` (module name is demo-scaffold) — never commit
# binaries. The new-service.sh wrapper runs the generator via `go run .` by
# default; set SCAFFOLD_BIN to a local build only if you want the speedup.
/demo-scaffold
/scaffold
/bin/
